<UserControl x:Class="GLLRV.DesktopApp.Views.Pages.ChamadosAndamentoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="CHAMADOS EM ANDAMENTO"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#222"
                   HorizontalAlignment="Center"
                   Margin="0,20,0,10"/>

        <Border Grid.Row="1"
                Margin="20"
                Background="#F9F9F9"
                CornerRadius="20"
                Padding="20">
            <DataGrid x:Name="ChamadosGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      ColumnHeaderHeight="35"
                      RowHeight="28">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="2*"/>
                    <DataGridTextColumn Header="Responsável" Binding="{Binding Responsavel}" Width="*"/>
                    <DataGridTextColumn Header="Abertura" Binding="{Binding DataAbertura, StringFormat=dd/MM/yyyy HH:mm}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
