<UserControl x:Class="GLLRV.DesktopApp.Views.Pages.ChamadosPendentesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">

    <Grid>

        <!-- FUNDO (se tiver a imagem, ajusta o Source) -->
        <Image Source="/Assets/background-gllrv.png"
               Stretch="Fill"
               Opacity="1"
               Panel.ZIndex="-1"/>

        <!-- CONTEÚDO CENTRAL -->
        <Grid Margin="260,40,40,40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TÍTULO -->
            <TextBlock Text="CHAMADOS PENDENTES"
                       Grid.Row="0"
                       HorizontalAlignment="Center"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#222"/>

            <!-- CABEÇALHO DAS COLUNAS -->
            <Border Grid.Row="2"
                    Margin="0,0,0,8"
                    Padding="16,8"
                    Background="#F2F2F2"
                    CornerRadius="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>   <!-- Usuário -->
                        <ColumnDefinition Width="2*"/>   <!-- Nível -->
                        <ColumnDefinition Width="2*"/>   <!-- Prioridade -->
                        <ColumnDefinition Width="2*"/>   <!-- Dificuldade -->
                        <ColumnDefinition Width="2*"/>   <!-- Data -->
                        <ColumnDefinition Width="2*"/>   <!-- Horário -->
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="USUÁRIO"             FontWeight="Bold" FontSize="12" Foreground="#333"/>
                    <TextBlock Grid.Column="1" Text="NÍVEL DE PRIORIDADE" FontWeight="Bold" FontSize="12" Foreground="#333"/>
                    <TextBlock Grid.Column="2" Text="DIFICULDADE"         FontWeight="Bold" FontSize="12" Foreground="#333"/>
                    <TextBlock Grid.Column="3" Text="DATA"                FontWeight="Bold" FontSize="12" Foreground="#333"/>
                    <TextBlock Grid.Column="4" Text="HORÁRIO"             FontWeight="Bold" FontSize="12" Foreground="#333"/>
                </Grid>
            </Border>

            <!-- LISTA DE CHAMADOS -->
            <DataGrid x:Name="ChamadosGrid"
                      Grid.Row="2"
                      Margin="0,40,0,0"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="None"
                      GridLinesVisibility="Horizontal"
                      Background="#CCFFFFFF"
                      BorderBrush="Transparent"
                      BorderThickness="0"
                      RowHeight="40">

                <DataGrid.Columns>
    <DataGridTextColumn Header="Usuário"
                        Binding="{Binding Usuario}"
                        Width="2*" />

    <DataGridTextColumn Header="Prioridade"
                        Binding="{Binding NivelPrioridade}"
                        Width="2*" />

    <DataGridTextColumn Header="Dificuldade"
                        Binding="{Binding Dificuldade}"
                        Width="2*" />

    <DataGridTextColumn Header="Data"
                        Width="2*"
                        Binding="{Binding DataAbertura, StringFormat=dd/MM/yyyy}" />

    <DataGridTextColumn Header="Horário"
                        Binding="{Binding HoraAbertura}"
                        Width="2*" />
</DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Grid>
</UserControl>
