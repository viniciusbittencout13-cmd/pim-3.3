<UserControl x:Class="GLLRV.DesktopApp.Views.Pages.ChamadosPendentesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>

       <Grid.Background>
    <ImageBrush ImageSource="/Assets/GLLRV.png"
                Stretch="UniformToFill"
                Opacity="0.25"/>
</Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CHAMADOS PENDENTES"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16"/>

        <DataGrid x:Name="ChamadosGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="USUÁRIO" Binding="{Binding Usuario}" Width="*"/>
                <DataGridTextColumn Header="NÍVEL DE PRIORIDADE" Binding="{Binding Prioridade}" Width="*"/>
                <DataGridTextColumn Header="DIFICULDADE" Binding="{Binding Dificuldade}" Width="*"/>
                <DataGridTextColumn Header="DATA" Binding="{Binding Data}" Width="*"/>
                <DataGridTextColumn Header="HORÁRIO" Binding="{Binding Horario}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
