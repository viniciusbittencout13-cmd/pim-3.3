<UserControl x:Class="GLLRV.DesktopApp.Views.Pages.HistoricoChamadosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    <Grid>
        <DataGrid x:Name="ChamadosGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Responsável" Binding="{Binding Responsavel}" Width="160"/>

                <DataGridTextColumn Header="Abertura" Width="140">
                    <DataGridTextColumn.Binding>
                        <Binding Path="DataAbertura" StringFormat="dd/MM/yyyy HH:mm"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Fechamento" Width="140">
                    <DataGridTextColumn.Binding>
                        <Binding Path="DataFechamento" StringFormat="dd/MM/yyyy HH:mm"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
